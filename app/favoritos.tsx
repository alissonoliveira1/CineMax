import { View, Text } from "react-native";
import Menu from "@/components/menu";
const Favoritos = () =>{
    return(
        <View style={{flex:1}}>
            <Text>Favoritos</Text>
            <Menu/>
        </View>
    )
}
export default Favoritos;