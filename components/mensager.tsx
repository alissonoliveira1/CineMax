import { useFocusEffect } from 'expo-router';
import { useCallback } from 'react';
import { Alert, Button, View } from 'react-native';

export const mensager = () => {
   

  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" , position: "absolute",top:100, zIndex: 50}}>
    
    </View>
  );
}
